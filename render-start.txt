# Mantener 1 worker mientras la reutilización explícita del adjunto anterior viva en caché de proceso.
# Si ese caché pasa a Redis/Postgres, se puede escalar workers sin perder ese contexto.
gunicorn --workers 1 --threads 1 --timeout 180 app:app
